fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.11
python-dotenv>=1.0
httpx>=0.28

# LLM providers -- only the one selected via LLM_PROVIDER needs to actually
# import successfully at runtime, but installing all of them keeps local
# dev friction-free when switching providers.
google-generativeai>=0.8
anthropic>=0.40
openai>=1.55
